export enum StepType {
    take_image = "take_image",
    unscrewing = "unscrewing"
}

export enum ImageScope {
    full_battery = "full_battery",
    section = "section"
}

export enum UnscrewingMode {
    automatic = "automatic",
    specific = "specific"
}